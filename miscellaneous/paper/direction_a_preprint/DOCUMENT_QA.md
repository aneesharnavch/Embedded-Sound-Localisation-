# Final document check

Both final PDFs were compiled from the accompanying LaTeX sources and rendered with Poppler. All nine main pages and all five supplementary pages were inspected visually, including equations, tables, figure labels, captions, line/page numbering, references and declarations. No clipping, missing citations, overflow or unreadable labels remained. Standard TeX underfull-box notices were reviewed visually; they do not conceal content.

The final text uses the corrected v2 results, 49 training-eligible scenes, and the exact prototype-order/total-order filter wording. The metadata, author name, self-funding, no affiliation and unreviewed status agree. Conflict confirmation, final author review and licensing are intentionally pending.

The structured abstract contains 167 whitespace-delimited words, below the 200-word target. Nine verified numbered references appear in citation order. The main paper contains six figures and three tables; the supplement contains one figure and two tables.

`reproduction/document_check.json` records final PDF hashes and page counts. The clean numerical reproduction is recorded separately in `reproduction/clean_environment_report.json`. This is internal document QA, not external peer review.
